import React from 'react';
import {View, Text, TouchableOpacity} from 'react-native';

const Button = ()=> {
  return <View>
    <TouchableOpacity >
      <Text>Button</Text>
    </TouchableOpacity>
  </View>
};

export default Button;